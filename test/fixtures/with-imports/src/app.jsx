// Synthetic source file for import-site scanning tests.
import flat from "array.prototype.flat";
const pad = require("left-pad");
import("array.prototype.flat/auto").then(() => {});

export function demo(rows) {
  return flat(rows);
}
